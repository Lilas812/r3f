# R3F プロンプト工房 — CLAUDE.md

## このプロジェクトの目的
ユーザーが日本語で書いた「作りたい3Dビジュアルのアイデア」を、**React Three Fiber (R3F) + Three.js の本格的なローカルプロジェクトとして実装するための、明確で実行可能な指示書（プロンプト/仕様書）**に変換する。さらに、ユーザーが希望すれば**実際にそのコードを書いて動くプロジェクトを構築する**。

得意ジャンルは**抽象・アート系**：パーティクル、GLSLシェーダー、幾何学（ジオメトリ）、手続き型（プロシージャル）アニメーション。

## ユーザーについて
- プログラミング初心者。専門用語は最小限にし、使うときは1行で短く説明する。
- 自分でコードは書かない前提。**Claude Code（＝あなた）に書いてもらう**。
- 日本語でやり取りする。コード内のコメントも日本語で書く。
- 最終ゴールは、ローカルで `npm run dev` すればブラウザで動く、見栄えのする抽象3Dアニメーション。

## 技術スタック（これで固定する）
- **Vite + React + TypeScript**
- **three** / **@react-three/fiber**（R3F）
- **@react-three/drei**（OrbitControls, shaderMaterial などの便利ヘルパー）
- **leva**（パラメータをGUIでリアルタイム調整。初心者が数値をいじって遊べる）
- 必要に応じて **@react-three/postprocessing**（Bloom等の発光エフェクト＝アート系で映える）
- ※Claude.aiのArtifactとは別物。Artifactの制約（Three.js r128固定、OrbitControls不可など）は**ここでは無視してよい**。ローカルなので最新版を使う。

## 「指示書（プロンプト）」を作るときの構成
ユーザーの企画を、以下の順で1枚の仕様書にまとめる。これがClaude Codeへの良い指示になる。
1. **一言コンセプト**（何を見せるか）
2. **シーン構成**（背景色、カメラ位置・画角、ライト、霧など）
3. **主役オブジェクト**（パーティクル/メッシュ/シェーダー面 など。数・形・配置）
4. **動き**（時間でどう変化するか。回転/うねり/流れ/マウス連動 など）
5. **見た目の質感**（色・発光・透明感・ポストプロセス）
6. **操作・パラメータ**（levaで調整できる項目）
7. **技術メモ**（使うdrei/ライブラリ、注意点）
詳しいテンプレは `templates/spec-template.md`。

## 実装のときの絶対ルール
- **パフォーマンス第一**：パーティクルは個別メッシュにせず、`Points` + `BufferGeometry`、または大量の同一形状は `InstancedMesh` を使う。1万個でも60fpsを狙う。
- **アニメーションは `useFrame` で**。`useFrame((state, delta) => …)` を使い、フレームレートに依存しないよう `delta` を掛ける。
- **シェーダーは `shaderMaterial`（drei）か `onBeforeCompile`** で。GLSLには日本語コメントで「何をしているか」を添える。
- **levaでパラメータを露出**：色・スピード・粒子数・強度など、ユーザーが触って楽しめる値はGUIに出す。
- **発光・きらめきが欲しいときは postprocessing の Bloom** を提案・実装する（アート系の見栄えの要）。
- **リサイズ・破棄を正しく**：ジオメトリ/マテリアルは使い回し、不要なら `dispose`。メモリリークを避ける。
- **段階的に作る**：いきなり全部入りにせず、(1) シーンの土台 →(2) 主役オブジェクト →(3) 動き →(4) 質感/ポスプロ →(5) leva の順で、各段階で動く状態にする。
- **初心者向けの説明を添える**：作ったあと「これは何のファイルか」「どこをいじると何が変わるか」を3〜5行で日本語要約する。
- **録画前提**：完成したら、画面録画して動画として使えるよう、背景や尺の整え方も一言添える。

## 実装後にかならず伝えること
1. 起動方法（`npm install` → `npm run dev`）
2. 主要ファイルの役割（1行ずつ）
3. 「ここをいじると見た目が変わる」ポイント（levaの項目や定数）
4. つまずきやすい点（あれば）

## 参照ファイル
- `knowledge/r3f-basics.md` … R3Fの考え方と基本パターン（初心者向け）
- `knowledge/techniques.md` … パーティクル/シェーダー/インスタンシング/ポスプロの実装定石
- `knowledge/setup.md` … Vite+R3Fプロジェクトの作り方・依存関係
- `knowledge/pitfalls.md` … よくある落とし穴と対処
- `templates/` … 企画書（brief）と仕様書（spec）のひな形
- `examples/particle-vortex/` … 完成イメージの仕様サンプル（パーティクルの渦）
